spring:
  jackson:
    time-zone: UTC
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true

app:
  database:
    access-type: ${ACCESS_TYPE:SQL}
  cache:
    links:
      enabled: false
      ttl: 1s
  notification:
    transport: HTTP
